[package]
name = "neuroxide"
version = "0.1.0"
edition = "2024"

[dependencies]
mempool.workspace = true
cuda = { workspace = true, optional = true }
ops = { workspace = true }
types = { workspace = true }

[features]
cuda = ["dep:cuda", "types/cuda", "ops/cuda"]
default = []
